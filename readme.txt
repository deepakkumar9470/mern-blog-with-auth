1. For Frontend(react-blog)
   a. Go ot react-blog folder then npm install
   b. npm start (To start the frontend)



2. For Backend(server)
   a. Go ot server folder then npm install
   b. Then add your env files url like mongodb,secret keys etc
   c. npm run dev (To start the server)
